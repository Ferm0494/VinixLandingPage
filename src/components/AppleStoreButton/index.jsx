import React from 'react';
import "./index.scss"
const AppleStoreButton = ({url}) => {
    return (
        <a href={url} id="IOSbutton">
            
        </a>
    );
}

export default AppleStoreButton;
