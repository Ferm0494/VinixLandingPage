import React from 'react';
import './index.scss'

const GoogleStoreButton = ({url}) => {
    return (
        <a href={url}  id="GooglePlayButtonStore">

            
        </a>
    );
}

export default GoogleStoreButton;
